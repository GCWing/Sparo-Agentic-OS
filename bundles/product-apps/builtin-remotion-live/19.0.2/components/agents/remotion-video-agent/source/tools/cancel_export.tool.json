{
  "name": "cancel_export",
  "description": "Cancel a running Remotion export job and preserve its terminal cancellation status.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["runId"],
    "properties": {
      "workspacePath": { "type": "string" },
      "runId": { "type": "string", "minLength": 1 }
    }
  },
  "runtime": "javascript",
  "entry": "tools/cancel_export.js",
  "readonly": false,
  "permissions": {},
  "timeoutMs": 60000,
  "maxOutputBytes": 60000
}
