{
  "name": "detect_project",
  "description": "Detect whether the current workspace is a HarmonyOS project and load bundle, module, ability, SDK, signing, and source hints.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "workspacePath": { "type": "string" }
    }
  },
  "runtime": "javascript",
  "entry": "tools/detect_project.js",
  "readonly": true,
  "permissions": {},
  "timeoutMs": 30000,
  "maxOutputBytes": 16000
}
